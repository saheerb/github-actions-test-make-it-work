name: PR Basic Checks

on:
  pull_request:
  push:

jobs:
  license-check:
    runs-on: ubuntu-latest

    steps:
      - 
        name: install dependencies
        shell: bash
        run: |
          time docker pull mbedos/mbed-os-env:latest
          ls
          
      - 
        name: install dependencies
        shell: bash
        run: |
          ls
          lsls
          ls
          
      
